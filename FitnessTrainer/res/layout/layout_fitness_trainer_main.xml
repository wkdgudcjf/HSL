<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/fitness_main_grey_bg" >

        <!-- 메인 Top Bar -->

        <include layout="@layout/layout_fitness_top_bar" />

        <!-- 서브 Top Bar -->

        <include layout="@layout/layout_fitness_trainer_main_sub_bar" />

        <!-- 회원 목록 리스트 -->
        <ListView
            android:id="@+id/fitness_memeber_main_listview"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
        
    </LinearLayout>

    <!-- 네비게이션 -->

    <include layout="@layout/layout_fitness_trainer_slide_menu" />
</android.support.v4.widget.DrawerLayout>
